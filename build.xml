<project name="rareshroom" basedir="." default="compile">

    <property name="src.dir"     location="src"/>
    <property name="lib.dir"     location="libs" />
    <property name="build.dir"   value="build"/>

    <path id="classpath">
        <fileset dir="${lib.dir}" includes="**/*.jar"/>
    </path>

    <target name="compile">
        <mkdir dir="build/classes"/>
        <javac srcdir="src" destdir="build/classes" classpathref="classpath" includeantruntime="false"/>
    </target>

    <target name="jar" depends="compile">
        <mkdir dir="${build.dir}/jar"/>
        <jar destfile="${build.dir}/jar/${ant.project.name}.jar" basedir="${build.dir}/classes">
            <manifest>
                <attribute name="Main-Class" value="${ant.project.name}.Main"/>
            </manifest>
        </jar>
    </target>

    <target name="run" depends="jar">
        <java classname="rareshroom.Main" classpath="${build.dir}/classes:${lib.dir}/truffle-api.jar">
<!--            <arg line="&#45;&#45;module-path=/home/octavel/PhD_Programs/my_programs/rareshroom/libs/truffle-api.jar"/>-->
<!--            <arg line="&#45;&#45;add-opens=org.graalvm.truffle/com.oracle.truffle.api=ALL-UNNAMED"/>-->
<!--            <arg line="&#45;&#45;add-opens=org.graalvm.truffle/com.oracle.truffle.api.interop=ALL-UNNAMED"/>-->
<!--            <arg line="&#45;&#45;add-opens=org.graalvm.truffle/com.oracle.truffle.api.nodes=ALL-UNNAMED"/>-->
        </java>
    </target>

    <target name="clean">
        <delete dir="${build.dir}/"/>
    </target>
</project>
